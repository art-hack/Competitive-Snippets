<snippet>
    <content><![CDATA[
ll power(ll x,ll y) { 
    if (y == 0) return 1;
    ll p =power(x, y / 2);
    if (y % 2 == 0) return p * p; 
    else return x * p * p; 
}

]]></content>
  <tabTrigger>POWER</tabTrigger>
</snippet>